# ledger
ai agent action history &amp; diffing

### idea:
a tool that users can attach to or wrap around their autonomous ai agent which defines and tracks their actions in such a way that they can be deterministically repeatable or reversible. for example, version history. git. diffing
